//
// Created by stevlulz on 4/20/18.
//

#ifndef MAXIMUM_MAX_SUB_ARRAY_H
#define MAXIMUM_MAX_SUB_ARRAY_H

#define LEN 100

/**
 * Maximum  SubArray
 * @arg1 a pointer to array
 * @arg2 result initilized to the minimum number (supposed 0 or we can use MinInt)
 * @arg3 tmp variable used for calculation must be initialized to zero or MinInt
 * @arg4 index used to traverse the array , initially = 0
 *
 */
void max_subarray(int*,int*,int*,int);

#endif //MAXIMUM_MAX_SUB_ARRAY_H
